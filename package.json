{
  "name": "keycloak-slickteam-theme",
  "version": "1.0.0",
  "scripts": {
    "env:start": "docker compose -f docker-compose.yml up -d",
    "env:stop": "docker compose -f docker-compose.yml down",
    "attach:psql": "docker exec -it -u postgres postgres psql"
  }
}
